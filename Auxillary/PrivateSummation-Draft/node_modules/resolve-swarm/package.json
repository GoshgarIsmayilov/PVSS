{
  "name": "resolve-swarm",
  "version": "0.1.0",
  "description": "resolve swarm",
  "main": "src/index.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "test": "mocha",
    "coverage": "nyc --reporter=html --reporter=text mocha",
    "report-coverage": "nyc report --reporter=text-lcov > coverage.lcov && codecov",
    "release": "npm version minor && npm publish",
    "patch": "npm version patch && npm publish"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/alincode/resolve-swarm.git"
  },
  "keywords": [
    "swarm"
  ],
  "author": "alincode <alincode@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "chai": "^4.2.0",
    "codecov": "^3.1.0",
    "mocha": "^5.2.0",
    "nyc": "^13.1.0",
    "solcjs-mock": "0.0.1"
  }
}
